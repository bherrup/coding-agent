apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - pvc.yaml
  - service.yaml

images:
  - name: us-central1-docker.pkg.dev/GCP_PROJECT_ID/your-artifact-registry-repo/fleet-agent
    newName: us-central1-docker.pkg.dev/GCP_PROJECT_ID/your-artifact-registry-repo/fleet-agent
    newTag: latest
